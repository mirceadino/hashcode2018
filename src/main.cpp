#include <bits/stdc++.h>
#include "input_class.h"

using namespace std;

int main(int argc, char **argv) {
    Input input;
    input.ReadFromFile("");
    Solve *solve = new MySolve(Input);
    return 0;
}